server {
    listen 8080;
    server_name localhost;
    # Matches the backend's five-file multipart ceiling (5 × 20 MiB + overhead).
    client_max_body_size 105m;

    include /etc/nginx/conf.d/locations.inc;
}
